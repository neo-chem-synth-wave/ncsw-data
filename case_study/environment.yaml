name: ncsw-data-case-study-env

channels:
  - conda-forge

dependencies:
  - notebook
  - pip
  - py7zr
  - rdkit
  - requests
  - seaborn
  - tqdm
  - pip:
      - ord-schema
      - pqdm
      - pyarrow
      - rdchiral
      - rxnmapper
